open Printf;;

# 21 "?"
let floats = 

# 4 "?"
let n = 1.234000 in

# 5 "?"
let m = (n +. 10.500000) in

# 6 "?"
printf  ("%f\n") (m);

# 7 "?"
let m = (n -. 1.100000) in

# 8 "?"
printf  ("%f\n") (m);

# 9 "?"
let m = (n *. 0.100000) in

# 10 "?"
printf  ("%f\n") (m);

# 12 "?"
let m = (n /. 0.100000) in

# 13 "?"
printf  ("%f\n") (m);

# 16 "?"
let m = (-. n) in
printf  ("%f\n") (m);

# 18 "?"
let m = (n ** 2.000000) in

# 19 "?"
printf  ("%f\n") (m)

