open Printf;;

# 22 "?"
let floats = 
# 21 "?"


# 5 "?"
let n = 
# 4 "?"
1.234000 in

# 6 "?"
let m = (
# 5 "?"
n +. 10.500000) in

# 6 "?"
printf  ("%f\n") (m);

# 8 "?"
let m = (
# 7 "?"
n -. 1.100000) in

# 8 "?"
printf  ("%f\n") (m);

# 10 "?"
let m = (
# 9 "?"
n *. 0.100000) in

# 10 "?"
printf  ("%f\n") (m);

# 13 "?"
let m = (
# 12 "?"
n /. 0.100000) in

# 13 "?"
printf  ("%f\n") (m);

# 16 "?"
let m = (-. n) in
printf  ("%f\n") (m);

# 19 "?"
let m = (
# 18 "?"
n ** 2.000000) in

# 19 "?"
printf  ("%f\n") (m)

